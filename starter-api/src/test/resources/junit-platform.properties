# Cucumber's own summary/pretty output is what shows scenario- and step-level pass/fail in the
# build log — Surefire/Failsafe's own "Tests run: N" counter does not drill into the Cucumber
# engine's dynamic test tree and always reports 0 for it (harmless, see AGENTS.md Gotchas).
cucumber.plugin=pretty,summary
